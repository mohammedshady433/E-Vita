<Page x:Class="E_Vita.Finance"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Finance" Height="1500" Width="850"
        Background="Transparent">


    <Border CornerRadius="12" BorderThickness="4">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0f4075" Offset="0"/>
                <GradientStop Color="#3283b8" Offset="0.75"/>
                <GradientStop Color="#Bbe1fe" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Border.Background>
            <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                <GradientStop Color="#3283b8" Offset="0"/>
                <GradientStop Color="#0f4075" Offset="0.75"/>
                <GradientStop Color="#1B1448" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="52"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="Finance"
                          Foreground="#f5efe5" FontSize="40" FontFamily="Qatar2022 Arabic Bold" Grid.Column="0"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Margin="60,5,0,0"/>
            </Grid>

            <WrapPanel
                       Height="100"
                       Grid.Row="1"
                       Orientation="horizontal"
                       Margin="0,15,0,0"
                       VerticalAlignment="Top">
                <Border CornerRadius="24" BorderThickness="4" Margin="25,0,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Color="#3283b8" Offset="0"/>
                            <GradientStop Color="#0f4075" Offset="1"/>

                        </LinearGradientBrush>
                    </Border.Background>


                    <StackPanel Width="250"
                           Height="100"
                          Margin="-4"
                           Orientation="Vertical"            >
                        <TextBlock Text="Revenue" FontSize="30" Margin="20,20,0,-10" FontFamily="Qatar2022 Arabic" FontWeight="DemiBold" Foreground="#f5efe5"></TextBlock>
                        <TextBlock Text="$15000" FontSize="24" Margin="20,10,0,0" FontFamily="Qatar2022 Arabic" FontWeight="DemiBold" Foreground="Orchid"></TextBlock>

                    </StackPanel>
                </Border>
                <Border CornerRadius="24" BorderThickness="4"  Margin="25,0,0,0" >


                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Color="#3283b8" Offset="0"/>
                            <GradientStop Color="#0f4075" Offset="1"/>

                        </LinearGradientBrush>
                    </Border.Background>


                    <StackPanel Width="250"
             Height="100"
             Margin="-4"
             Orientation="Vertical"            >
                        <TextBlock Text="Expenses" FontSize="30" Margin="20,20,0,-10" FontFamily="Qatar2022 Arabic" FontWeight="DemiBold" Foreground="#f5efe5"></TextBlock>
                        <TextBlock Text="$15000" FontSize="24" Margin="20,10,0,0" FontFamily="Qatar2022 Arabic" FontWeight="DemiBold" Foreground="OrangeRed"></TextBlock>

                    </StackPanel>
                </Border>
                <Border CornerRadius="24" BorderThickness="4"  Margin="25,0,0,0">


                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Color="#3283b8" Offset="0"/>
                            <GradientStop Color="#0f4075" Offset="1"/>

                        </LinearGradientBrush>
                    </Border.Background>


                    <StackPanel Width="250"
             Height="100"
             Margin="-4"
             Orientation="Vertical"            >
                        <TextBlock Text="Profit" FontSize="30" Margin="20,20,0,-10" FontFamily="Qatar2022 Arabic" FontWeight="DemiBold" Foreground="#f5efe5"></TextBlock>
                        <TextBlock Text="$15000" FontSize="24" Margin="20,10,0,0" FontFamily="Qatar2022 Arabic" FontWeight="DemiBold" Foreground="LawnGreen"></TextBlock>

                    </StackPanel>
                </Border>
            </WrapPanel>

            <WrapPanel
           Height="400"
           Grid.Row="1"
           Orientation="horizontal"
           Margin="21,200,21,0"
           VerticalAlignment="Top">
                <Border CornerRadius="24" BorderThickness="4" Width="790" Height="451">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Color="#3283b8" Offset="0"/>
                            <GradientStop Color="#0f4075" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel 
                   Width="750"
                   Height="400"
                   Margin="-4"
                   Orientation="Vertical"            >
                        <!-- Chart for Revenue, Expenses, and Profit -->
                        <lvc:CartesianChart x:Name="RevenueExpensesChart"  Height="371" Width="734">
                            <lvc:CartesianChart.Series>
                                <!-- Revenue Line -->
                                <lvc:LineSeries Title="Revenue" Values="{Binding RevenueValues}" Stroke="Orchid" Fill="Transparent" />
                                <!-- Expenses Line -->
                                <lvc:LineSeries Title="Expenses" Values="{Binding ExpensesValues}" Stroke="OrangeRed" Fill="Transparent" />
                                <!-- Profit Line -->
                                <lvc:LineSeries Title="Profit" Values="{Binding ProfitValues}" Stroke="LawnGreen" Fill="Transparent" />
                            </lvc:CartesianChart.Series>
                            <!-- Axis for X and Y -->
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Month" FontFamily="Qatar2022 Arabic" FontSize="15" Labels="{Binding MonthLabels}" />
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Amount (in $)"  FontFamily="Qatar2022 Arabic" FontSize="15" LabelFormatter="{Binding YFormatter}" />
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>

            </WrapPanel>

            <WrapPanel
Height="800"
Grid.Row="1"
Orientation="horizontal"
Margin="21,633,21,0"
VerticalAlignment="Top">
                <Border CornerRadius="24" BorderThickness="4"  Margin="0,20,0,0" Width="790">


                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Color="#3283b8" Offset="0"/>
                            <GradientStop Color="#0f4075" Offset="1"/>

                        </LinearGradientBrush>
                    </Border.Background>


                    <StackPanel 
        Width="750"
        Height="800"
        Margin="-4"
        Orientation="Vertical" >
                        <DataGrid x:Name="TransactionsDataGrid" AutoGenerateColumns="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                Background="Transparent" RowBackground="DimGray" AlternatingRowBackground="SlateGray"  Foreground="#1b262c"
                BorderBrush="Transparent" FontFamily="Qatar2022 Arabic" FontSize="28" 
               ItemsSource="{Binding Transactions}" Height="766">
                            <!-- Define Columns -->
                            <DataGrid.Columns>
                                <!-- Date Column -->
                                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"  />
                                <!-- Description Column -->
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                                <!-- Amount Column -->
                                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*" />
                                <!-- Type Column -->
                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

            </WrapPanel>


        </Grid>

    </Border>
</Page>
