<Page x:Class="E_Vita.Nurse_Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1532"
      Title="Nurse Dashboard">
    <Grid Background="#ecece4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="6" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <StackPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#34111e" Offset="-0.5"/>
                    <GradientStop Color="#891739" Offset="0.5"/>
                    <GradientStop Color="#991130" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <TextBlock Text="Nurse Dashboard" Foreground="#e1b184" FontSize="56" FontFamily="Qatar2022 Arabic" FontWeight="Heavy" 
                       VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,50,0,0"/>
        </StackPanel>

        <!-- Side Panel -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="6" Height="800" VerticalAlignment="Bottom">
            <StackPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#34111e" Offset="-0.5"/>
                    <GradientStop Color="#891739" Offset="0.5"/>
                    <GradientStop Color="#991130" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <!--logo-->
            <Image Source="/Assets/E-Vita logo-01.png" Height="143" Margin="0,40,0,20" Width="265"/>
            <Button Content="Book Appointment" Click="BookAppointment_Click" FontFamily="Qatar2022 Arabic" FontSize="14" Padding="10"
                    Background="#e1b184" Foreground="#891739" Width="166" Height="46" Margin="5,150,5,10">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="166" Height="46" CornerRadius="20" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Add Patient" Click="AddPatient_Click" FontFamily="Qatar2022 Arabic" FontSize="14" Padding="10"
                    Background="#e1b184" Foreground="#891739" Width="166" Height="46" Margin="5,50,5,10">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="166" Height="46" CornerRadius="20" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="" Click="LogOut_Click" FontFamily="Qatar2022 Arabic" FontSize="14" Padding="10" Foreground="#891739" Width="166" Height="46" Margin="5,240,5,10">
                <Button.Background>
                    <ImageBrush ImageSource="/Views/rb_5844.png" Stretch="Uniform"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="166" Height="46" CornerRadius="20" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- Schedule -->
        <Border Grid.Column="1" Grid.Row="1" Grid.RowSpan="5" Grid.ColumnSpan="3" Background="#f9dcc0" CornerRadius="10" Margin="10">
            <Grid>
                <UniformGrid x:Name="CalendarGrid" Rows="7" Columns="7" Margin="10">
                    <!-- Days and buttons will be populated dynamically in code-behind -->
                </UniformGrid>

                <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <Button Content="◀" Width="30" Height="30" Background="#e1b184" Foreground="#891739"
                                    FontSize="16" FontWeight="Bold" BorderThickness="0"
                                    Click="PreviousMonth_Click" />
                    <ComboBox x:Name="YearSelector" Width="150" Height="30" SelectionChanged="YearSelector_SelectionChanged" Margin="10,0">
                        <!-- Populate years dynamically in code-behind -->
                    </ComboBox>
                    <ComboBox x:Name="MonthSelector" Width="200" Height="30" SelectionChanged="MonthSelector_SelectionChanged" Margin="10,0">
                        <!-- Populate months dynamically in code-behind -->
                    </ComboBox>
                    <Button Content="▶" Width="30" Height="30" Background="#e1b184" Foreground="#891739"
                                    FontSize="16" FontWeight="Bold" BorderThickness="0"
                                    Click="NextMonth_Click" />
                </StackPanel>
            </Grid>
        </Border>
        <!-- Schedule -->

        <StackPanel Grid.Column="4" Grid.ColumnSpan="3" Grid.Row="1" Margin="4,20,10,2" Grid.RowSpan="5">
            <ListBox x:Name="ScheduleList" Height="600" SelectionChanged="ScheduleList_SelectionChanged" Background="#f9dcc0" BorderBrush="#f9dcc0">
                <ListBox BorderBrush="#f9dcc0" Background="#f9dcc0">
                    <DataGrid x:Name="ScheduleDataGrid" Grid.Column="1" Margin="0" AutoGenerateColumns="False" Height="580" Width="624" Background="#e1b184" BorderBrush="#FFBBE1FA"  RowStyle="{DynamicResource DataGridRowStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" Style="{DynamicResource DataGridStyle1}" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time }" Width="*" />
                            <DataGridTextColumn Header="Patient Name" Binding="{Binding Patient_appointment.name}" Width="*"/>
                            <DataGridTextColumn Header="Patient Phone" Binding="{Binding Patient_appointment.contact}" Width="*"/>
                            <DataGridTextColumn Header="Patient ID" Binding="{Binding Patient_appointment.Patient_ID}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ListBox>
            </ListBox>
        </StackPanel>

    </Grid>
</Page>
